# Pedro's hosted fork is deliberately Local Farm only. Leaving both values empty
# prevents this modded client from connecting to the upstream Online Farm service.
VITE_API_URL=
VITE_GOOGLE_CLIENT_ID=
